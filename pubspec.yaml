name: flutter_take_image_camera
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^0.1.3
  path_provider: ^0.4.1
  image_picker: ^0.6.4
  compressimage: ^0.1.1
  flutter_exif_rotation: ^0.2.7

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true

